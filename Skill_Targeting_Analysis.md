# 技能卡片目標範圍與參數分析報告

本文件詳細分析 `SkillCard` 中各項參數（Scope Type, AOE Radius 等）如何交互作用，並定義了它們對實際遊戲中技能施放範圍與影響區域的具體影響。

**注意**：`range_limit` 參數已被移除。現在所有非自身中心的技能，其施法距離皆為**無限（全圖可選）**。

## 核心概念：施法範圍 vs 影響範圍

在理解參數之前，必須區分兩個概念：

1.  **施法範圍 (Casting Range)**：
    *   **無限 (Global)**。
    *   玩家可以點選棋盤上**任何一個格子**作為技能的「中心點 (Center Cell)」。
    *   如果是 `origin_is_self = true`，則中心點強制為施法者。

2.  **影響範圍 (Scope / AOE)**：
    *   由 `scope_type` 和 `aoe_radius` 控制。
    *   決定了以「中心點」為基準，**周圍哪些格子會受到影響**。

---

## 參數詳解

| 參數名稱 | 類型 | 說明 |
| :--- | :--- | :--- |
| **scope_type** | Enum | 決定影響區域的形狀（單體、圓形、方形、全圖等）。 |
| **aoe_radius** | int | 影響區域的大小半徑。 |
| **origin_is_self** | bool | 若勾選，技能中心點強制鎖定為「施法者腳下」，無視滑鼠點選位置。 |
| **target_filter** | Enum | 在影響範圍內，篩選誰會中招（敵人、隊友、所有）。 |
| **can_target_empty** | bool | 決定玩家是否可以點擊空地發動技能。 |

---

## Scope Type 與參數交互分析

以下列出各 Scope Type 在不同 `aoe_radius` 下的表現：

### 1. SINGLE (單體)
*   **形狀**：單一格子。
*   **aoe_radius 影響**：**無影響** (始終為 1 格)。
*   **典型用途**：普通攻擊、單體治療、單體狙擊。
*   **示意圖**：
    ```
    [ ] [ ] [ ]
    [ ] [X] [ ]  <-- X 是目標點
    [ ] [ ] [ ]
    ```

### 2. AREA_CIRCLE (菱形/曼哈頓圓)
*   **形狀**：在網格系統中表現為**菱形**。判斷標準為 `abs(x) + abs(y) <= radius`。
*   **aoe_radius 影響**：
    *   `Radius = 1`：十字形（中心 + 上下左右各 1 格）。
    *   `Radius = 2`：擴大的菱形（中心向外延伸 2 格）。
*   **典型用途**：手榴彈、火球術 (擴散傷害)。
*   **示意圖 (Radius 1)**：
    ```
      [X]
    [X][C][X]
      [X]
    ```

### 3. AREA_SQUARE (方形)
*   **形狀**：以中心點為基準的正方形。
*   **aoe_radius 影響**：
    *   `Radius = 1`：**3x3** 區域 (包含中心)。
    *   `Radius = 2`：**5x5** 區域。
    *   公式：邊長 = `radius * 2 + 1`。
*   **典型用途**：暴風雪、大範圍區域魔法。
*   **示意圖 (Radius 1)**：
    ```
    [X][X][X]
    [X][C][X]
    [X][X][X]
    ```

### 4. AREA_CROSS (十字)
*   **形狀**：以中心點向上下左右延伸的直線。
*   **aoe_radius 影響**：決定延伸的長度（臂長）。
    *   `Radius = 3`：向四個方向各延伸 3 格。
*   **典型用途**：十字雷射、縱橫劍氣。
*   **示意圖 (Radius 2)**：
    ```
          [X]
          [X]
    [X][X][C][X][X]
          [X]
          [X]
    ```

### 5. AREA_QUEEN (米字型)
*   **形狀**：十字 (Cross) 加上 X 字 (Diagonal)，共 8 個方向。
*   **aoe_radius 影響**：
    *   若 `Radius > 0`：限制延伸長度。
    *   若 `Radius <= 0` (或預設)：通常視為無限延伸或覆蓋全圖射線。
*   **典型用途**：全方位爆發、向四周發射飛彈。
*   **示意圖**：
    ```
    [X] [ ] [X]
       [X] [X]
    [ ][X][C][X][ ]
       [X] [X]
    [X] [ ] [X]
    ```

### 6. GLOBAL (全圖)
*   **形狀**：地圖上**所有**存在的實體（受 Filter 影響）。
*   **aoe_radius 影響**：**無影響**。
*   **典型用途**：全體補血、天氣效果、全場地震。

### 7. GLOBAL_CHECKER_A / B (棋盤格)
*   **形狀**：類似國際象棋棋盤的黑格或白格分佈。
    *   `A`：(x+y) 為偶數的格子。
    *   `B`：(x+y) 為奇數的格子。
*   **aoe_radius 影響**：**無影響**。
*   **典型用途**：特殊機制技能、場地機關。

---

## 特殊參數組合案例

### 案例 A：自身周圍爆發 (如：旋風斬)
*   **origin_is_self**: `true` (勾選)
*   **scope_type**: `AREA_CIRCLE` 或 `AREA_SQUARE`
*   **aoe_radius**: `1`
*   **結果**：以施法者為中心，對周圍一圈單位造成影響。

### 案例 B：遠程狙擊 (如：精準射擊)
*   **origin_is_self**: `false`
*   **scope_type**: `SINGLE`
*   **aoe_radius**: (無影響)
*   **結果**：玩家可以點選全圖任意單一敵人。

### 案例 C：遠程火球 (AOE)
*   **origin_is_self**: `false`
*   **scope_type**: `AREA_CIRCLE` (菱形)
*   **aoe_radius**: `1` (爆炸範圍)
*   **can_target_empty**: `true` (允許點地板)
*   **結果**：玩家可以將火球丟到全圖任意位置（包含空地），落地後炸該格子及周圍上下左右各 1 格。

### 案例 D：全圖隊友治療
*   **scope_type**: `GLOBAL`
*   **target_filter**: `ALLY`
*   **結果**：地圖上所有己方單位回血。

---

## 總結流程圖

1.  **判定中心點**：
    *   若 `origin_is_self == true` -> 中心 = 施法者。
    *   若 `false` -> 中心 = 玩家滑鼠點選的格子 (全圖可選)。

2.  **計算影響格子**：
    *   根據 `scope_type` 和 `aoe_radius`，從中心點向外擴散，找出所有「範圍內」的格子座標。

3.  **篩選目標**：
    *   檢查這些格子上是否有單位 (`GridEntity`)。
    *   根據 `target_filter` (ALLY/ENEMY) 決定是否對該單位生效。

4.  **執行效果**：
    *   對篩選出的單位執行 Damage/Heal/Status。
